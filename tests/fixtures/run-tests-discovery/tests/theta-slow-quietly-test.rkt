#lang racket/base

;; @suite default
;; @speed slow
;; @boundary integration
;; @isolation process
;;
;; W5 frozen discovery fixture: slow speed metadata on a name that carries
;; NO slow heuristic pattern — proves the slow classifier reads metadata.
;; FIXTURE DATA — never executed by collection; copied to a temp root by
;; tests/test-run-tests-shard.rkt.
