#lang racket/base

;; @suite unit
;; @speed fast
;; @boundary unit
;; @mutates none
;;
;; W5 frozen discovery fixture: explicitly fast metadata. Name carries no
;; classifier pattern, so only the metadata above selects this file.
;; FIXTURE DATA — never executed by collection; copied to a temp root by
;; tests/test-run-tests-shard.rkt.
