#lang racket/base

;; @suite default
;; @speed fast
;;
;; W5 frozen discovery fixture: nested-directory case (one level).
;; FIXTURE DATA — never executed by collection; copied to a temp root by
;; tests/test-run-tests-shard.rkt.
