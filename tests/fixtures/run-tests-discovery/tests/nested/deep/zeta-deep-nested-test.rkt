#lang racket/base

;; @suite default
;; @speed fast
;;
;; W5 frozen discovery fixture: deeply nested case (two levels), proving
;; multi-level directory traversal with deterministic path output.
;; FIXTURE DATA — never executed by collection; copied to a temp root by
;; tests/test-run-tests-shard.rkt.
