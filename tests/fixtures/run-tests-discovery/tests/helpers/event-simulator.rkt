#lang racket/base

;; W5 frozen discovery fixture support module: lives under tests/helpers/
;; so the support-module exclusion keeps it out of every gate.
;; FIXTURE DATA — never executed by collection; copied to a temp root by
;; tests/test-run-tests-shard.rkt.
