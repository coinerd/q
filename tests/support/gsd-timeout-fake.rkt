#lang racket/base

;; Deprecated scratch stub (W4). The shared deterministic fake lives at
;; tests/helpers/gsd-timeout-fake.rkt; this file intentionally does nothing
;; and is kept only because shell deletion is policy-blocked.
