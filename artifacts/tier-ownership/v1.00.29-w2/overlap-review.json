{"milestone":"v1.00.29","note":"no platform/fast or security/fast intersecting test is byte-identical in execution context: fast runs the prepared-environment shard while platform (raco make full-install) and security (STRICT_TEST_RUNNER full-install) run cold contexts; security/fast rows additionally own required blocking security semantics and are never removable.","overlap_axes":["platform/fast","security/fast"],"removals":0,"rows":[{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-capability-aware-spawn.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-cell-diff-render.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-cli-flags.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-cli.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-config-paths.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-cwd-independence.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-error-classify.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-execution-plane-error-label.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-export-formats.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-extension-tiers.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-facade-surface.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-frontmatter-extended.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-llm-error-visibility.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-llm-model.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-lockfile.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-loop-cancellation.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-loop-edge-cases.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-loop-events.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-message-layout.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-metrics-helpers.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-mutating-tool-taxonomy.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-red-module-boundaries.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-runtime-packages.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-safe-mode.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-settings.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-spawn-subagent-serialization.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-streaming-tool-bug.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-streaming-transitions.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-subprocess-helpers.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-token-estimate-cache.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-truncation.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-verifier-gate.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-version.rkt"},{"axis":"platform/fast","decision":"kept","rationale":"kept: the platform gate re-executes this suite in a cold full-install environment on a distinct runner context; removing it from the platform gate would be a platform re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional platform/fast overlap, not an exact duplicate.","test":"tests/test-worker-security.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-approval-broker.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-approval-channel-teardown.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-approval-channel.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-approval-correlation.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-browser-permission-tiers.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-browser-safe-mode.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-capability-token-hardening.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-capability-tokens.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-destructive-warning.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-explorer-redaction-boundaries.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-generic-tool-approval.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-hitl-approval-integration.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-legacy-approval-api.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-mcp-protocol-compliance.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-mcp-security-gates.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-mutating-tool-taxonomy.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-oauth-callback-security.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-permission-bash-w1.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-permission-denial-path.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-permission-gate.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-permission-mode-wiring.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-permission-policy-mode.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-property-security.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-provider-config-redaction.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-provider-differential-fixtures.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-provider-tool-transport.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-routing-policy-integration.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-rpc-credential-redaction.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-safe-mode-defense-depth.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-safe-mode.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-scheduler-safe-mode.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-session-id-containment.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-session-production-cutover.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-spawn-approval-single-gate.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-spawn-approval.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-spawn-execution-plan.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-spawn-subagents-approval.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-test-sandbox.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-tool-bash-security-edges.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-tool-bash-security.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-tool-gateway-remote-routing.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-tool-internal-gate.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-trace-logger-containment.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-w2-credential-redaction.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/test-worker-security.rkt"},{"axis":"security/fast","decision":"kept","rationale":"kept: the security gate is a required blocking control that re-executes this suite under STRICT_TEST_RUNNER=1 in a cold full-install environment; removing it from the security gate would be a security re-tiering. The fast/prepared-environment run is a complementary shard, so this is an intentional security/fast overlap, not an exact duplicate.","test":"tests/workflows/test-sandbox-integration.rkt"}],"schema":"tier-ownership-overlap-review/v1","source_matrix":"artifacts/tier-ownership/v1.00.29-w0/ownership-matrix.json","wave":"v1.00.29-w2"}
