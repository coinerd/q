[
  {
    "sha": "06d531614edff5764f5d085f1a0a813ef999e51e",
    "filename": ".github/actions/setup-racket/action.yml",
    "status": "modified",
    "additions": 96,
    "deletions": 0,
    "changes": 96,
    "blob_url": "https://github.com/coinerd/q/blob/1351fb0568d349b96d3aa9083138138419e38548/.github%2Factions%2Fsetup-racket%2Faction.yml",
    "raw_url": "https://github.com/coinerd/q/raw/1351fb0568d349b96d3aa9083138138419e38548/.github%2Factions%2Fsetup-racket%2Faction.yml",
    "contents_url": "https://api.github.com/repos/coinerd/q/contents/.github%2Factions%2Fsetup-racket%2Faction.yml?ref=1351fb0568d349b96d3aa9083138138419e38548",
    "patch": "@@ -5,10 +5,92 @@ inputs:\n     description: 'Racket version to install'\n     required: false\n     default: '8.10'\n+  prepared-environment:\n+    description: >-\n+      'auto' lets this action restore a verified prepared environment\n+      (producer artifact consumed via a needs producer in the calling\n+      workflow) and skip the redundant Racket install and raco setup\n+      compile below; 'off' (default) keeps the full per-job path.\n+      Additional guards inside: Linux runners only and a non-empty\n+      prepared-installer-sha256. The repository-level rollback switch\n+      RACKET_PREPARED_ARTIFACT=off is evaluated by opting call sites\n+      (it keeps this input at 'off').\n+    required: false\n+    default: 'off'\n+  prepared-artifact-name:\n+    description: 'Prepared-environment artifact name to restore when prepared-environment=auto'\n+    required: false\n+    default: 'prepared-env-fast'\n+  prepared-installer-sha256:\n+    description: >-\n+      Published SHA-256 of the official installer archive; required by\n+      the restore path when prepared-environment=auto (empty disables\n+      the prepared path and falls through to the full path).\n+    required: false\n+    default: ''\n+outputs:\n+  prepared-env-result:\n+    description: >-\n+      Outcome of the guarded prepared-environment restore: 'success'\n+      (manifest-verified restore; install+compile skipped),\n+      'failure' (restore mismatched or failed; this action fell back to\n+      the full path and flagged the fallback), or 'skipped' (prepared\n+      path not requested for this call site).\n+    value: ${{ steps.prepared-env.outcome }}\n runs:\n   using: 'composite'\n   steps:\n+    # ------------------------------------------------------------------\n+    # W3 prepared-environment restore (guarded; ubuntu fast-gate lanes\n+    # only, docs/reports/CI-RACKET-CACHE-POLICY.md lane split). A\n+    # manifest-verified restore replaces the install+compile steps below\n+    # for this job; anything else \u2014 mismatch, missing artifact, or a call\n+    # site that did not opt in \u2014 runs the FULL path below, and a fallback\n+    # after a failed restore is flagged, never a silent rebuild. macOS\n+    # lanes cannot opt in: the runner.os guard plus the per-call-site\n+    # default 'off' keep the v1.00.10 compile-on-every-hit condition.\n+    # ------------------------------------------------------------------\n+    - name: Restore verified prepared environment (guarded)\n+      id: prepared-env\n+      if: ${{ inputs.prepared-environment == 'auto' && runner.os == 'Linux' && inputs.prepared-installer-sha256 != '' }}\n+      continue-on-error: true\n+      uses: ./.github/actions/restore-racket-environment\n+      with:\n+        racket-version: ${{ inputs.racket-version }}\n+        installer-sha256: ${{ inputs.prepared-installer-sha256 }}\n+        artifact-name: ${{ inputs.prepared-artifact-name }}\n+\n+    - name: Prepared-environment fallback marker (rebuilt, never silent)\n+      if: steps.prepared-env.outcome == 'failure'\n+      shell: bash\n+      run: |\n+        set -euo pipefail\n+        echo \"::warning::prepared-environment restore failed or mismatched; this job falls back to the full setup path and the artifact is marked as rebuilt (the next producer run re-uploads it)\"\n+        {\n+          echo \"## Racket package setup \u2014 prepared environment REBUILT (fallback)\"\n+          echo \"- Restore outcome: failure \u2014 this job ran the full install+compile path; the producer rebuilds the artifact on its next run. Fallback is flagged, never silent (docs/reports/CI-RACKET-CACHE-POLICY.md).\"\n+        } >> \"$GITHUB_STEP_SUMMARY\"\n+\n+    - name: Prepared-environment restored (verified) \u2014 install and compile skipped\n+      if: steps.prepared-env.outcome == 'success'\n+      shell: bash\n+      env:\n+        RESTORE_MS: ${{ steps.prepared-env.outputs.restore-ms }}\n+        LOCK_DIGEST: ${{ steps.prepared-env.outputs.lock-digest }}\n+      run: |\n+        set -euo pipefail\n+        {\n+          echo \"## Racket package setup \u2014 prepared environment restored\"\n+          echo \"- Restore outcome: success (manifest verified against this job's tuple; lock digest \\`${LOCK_DIGEST}\\`)\"\n+          echo \"- Restore wall clock: ${RESTORE_MS:-n/a} ms\"\n+          echo \"- Racket install + package-visible compile skipped per the ubuntu fast-gate lane split (docs/reports/CI-RACKET-CACHE-POLICY.md); the restore's four read-only health checks replace them.\"\n+        } >> \"$GITHUB_STEP_SUMMARY\"\n+\n+    # The steps below are the FULL path. They are skipped only when the\n+    # guarded restore above succeeded for this job; every other run \u2014\n+    # including every exact addon-cache hit \u2014 still compiles.\n     - name: Clean stale q workspace bytecode\n+      if: steps.prepared-env.outcome != 'success'\n       shell: bash\n       run: |\n         set -euo pipefail\n@@ -24,6 +106,7 @@ runs:\n     # bytecode from the fresh checkout.\n     - name: Configure Racket user package store\n       id: racket-store\n+      if: steps.prepared-env.outcome != 'success'\n       shell: bash\n       run: |\n         set -euo pipefail\n@@ -36,6 +119,7 @@ runs:\n     # restore a potentially incompatible package graph through a prefix key.\n     - name: Restore locked Racket user package store\n       id: racket-addon-cache\n+      if: steps.prepared-env.outcome != 'success'\n       uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25\n       with:\n         path: ${{ steps.racket-store.outputs.addon_path }}\n@@ -44,6 +128,7 @@ runs:\n           ${{ hashFiles('info.rkt', 'ci/racket-package-lock.rktd') }}\n \n     - name: Install Racket\n+      if: steps.prepared-env.outcome != 'success'\n       uses: Bogdanp/setup-racket@2466913449df77df2bad149d1f2fc4e1ea4795dd\n       with:\n         architecture: 'x64'\n@@ -63,6 +148,7 @@ runs:\n     # every q collection without rebuilding documentation.\n     # ------------------------------------------------------------------\n     - name: Link q and compile package-visible modules\n+      if: steps.prepared-env.outcome != 'success'\n       shell: bash\n       run: |\n         set -euo pipefail\n@@ -138,6 +224,16 @@ runs:\n         # package used by racket-tooling write validation. `fmt` is a build\n         # dependency, so q setup alone does not reliably install its `raco`\n         # command in a clean CI user store.\n+        #\n+        # Lane split (v1.00.16 W3, docs/reports/CI-RACKET-CACHE-POLICY.md):\n+        # this full install+compile path is the FALLBACK path for the\n+        # ubuntu fast-gate test shards (the guarded restore step at the\n+        # top of this action carries the prepared path). On a verified\n+        # prepared-environment restore this whole step is skipped; on\n+        # mismatch or producer failure the restore step above routes here\n+        # AND marks the artifact as rebuilt \u2014 never silently. The macOS\n+        # platform lane keeps compile-on-every-hit unconditionally per the\n+        # v1.00.10 release condition recorded in the policy doc.\n         echo \"::group::Compiling q collections and the formatter command\"\n         raco setup --no-docs --jobs 4 --pkgs q fmt\n         raco fmt --help >/dev/null"
  },
  {
    "sha": "4b89c234e2017865631e2137e3ec54b59d99ae05",
    "filename": ".github/workflows/ci.yml",
    "status": "modified",
    "additions": 110,
    "deletions": 16,
    "changes": 126,
    "blob_url": "https://github.com/coinerd/q/blob/1351fb0568d349b96d3aa9083138138419e38548/.github%2Fworkflows%2Fci.yml",
    "raw_url": "https://github.com/coinerd/q/raw/1351fb0568d349b96d3aa9083138138419e38548/.github%2Fworkflows%2Fci.yml",
    "contents_url": "https://api.github.com/repos/coinerd/q/contents/.github%2Fworkflows%2Fci.yml?ref=1351fb0568d349b96d3aa9083138138419e38548",
    "patch": "@@ -1,16 +1,25 @@\n name: CI\n \n-# W3 prepared-environment note (docs/reports/prepared-environment-pilot-v1.00.11.md):\n-# The 26 setup-racket call sites below are the ONLY default path for this\n-# workflow and remain unchanged until the recorded go decision. The\n-# prepared-environment pilot is report-only (.github/workflows/\n-# prepared-environment-pilot.yml) and never gates anything here. Rollback\n-# switch for any future cutover: repository variable RACKET_PREPARED_ARTIFACT=off\n-# restores exactly this per-job path with no change to test selection or L4\n-# semantics.\n+# W3 prepared-environment cutover (docs/reports/prepared-environment-pilot-v1.00.11.md\n+# + docs/reports/CI-RACKET-CACHE-POLICY.md):\n+# The ubuntu fast-gate `test` shards below restore the verified prepared\n+# environment produced by the `fast-env` job (addon-store/ + q-compiled/ +\n+# manifest.json, verified against the job tuple before any test) and fall\n+# back \u2014 loudly, never silently \u2014 to the full setup-racket install+compile\n+# path when the producer is skipped, fails, or the restore mismatches; the\n+# fallback marks the artifact as rebuilt. All other setup-racket call sites\n+# keep the per-job full path. macOS platform lanes keep compile-on-every-hit\n+# (v1.00.10 release condition). Rollback switch: repository variable\n+# RACKET_PREPARED_ARTIFACT=off restores exactly the per-job path with no\n+# change to test selection or L4 semantics.\n \n env:\n   FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true\n+  # Canonical SHA-256 of the official Racket 8.10 x64/full/CS Linux\n+  # installer archive, shared by the prepared-environment producer\n+  # (fast-env) and the guarded restore inside setup-racket. Mirrors\n+  # .github/workflows/prepared-environment-pilot.yml.\n+  INSTALLER_SHA_LINUX_8_10: '497f0f518fa4c5f8529269580a6a79366f21315af26cf3ed1ae8b47e05ceac72'\n \n on:\n   push:\n@@ -102,36 +111,102 @@ jobs:\n         run: racket scripts/run-tests/classify-metadata.rkt --lint-metadata\n \n   # \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n-  # Gate 2: Sharded test suite \u2014 3 parallel shards on ubuntu\n+  # Gate 1b: Prepared environment producer (v1.00.16 W3 cutover)\n+  # Builds the immutable per-run artifact (addon-store/ + q-compiled/ +\n+  # manifest.json) the ubuntu fast-gate `test` shards restore instead of\n+  # paying per-shard Racket install + raco setup compile cost. The\n+  # manifest is verified on restore against the job tuple before any\n+  # test runs (see docs/reports/CI-RACKET-CACHE-POLICY.md). Skipped on\n+  # workflow_dispatch (racket-version override \u21d2 shards fall back to the\n+  # full path) and by the rollback switch RACKET_PREPARED_ARTIFACT=off.\n+  # \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n+  fast-env:\n+    needs: lint\n+    if: ${{ github.event_name != 'workflow_dispatch' && vars.RACKET_PREPARED_ARTIFACT != 'off' }}\n+    runs-on: ubuntu-latest\n+    steps:\n+      - uses: actions/checkout@v7\n+      - uses: ./.github/actions/prepare-racket-environment\n+        with:\n+          racket-version: '8.10'\n+          installer-sha256: ${{ env.INSTALLER_SHA_LINUX_8_10 }}\n+          artifact-name: prepared-env-fast\n+\n+  # \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n+  # Gate 2: Sharded test suite \u2014 3 parallel shards on ubuntu (default)\n   # Replaces the old single-job full-suite + redundant inter-wave-gate.\n   # All 1039+ fast tests still run; sharding cuts wall-clock 3\u00d7.\n+  # W3: shards restore the prepared environment produced by fast-env\n+  # through the guarded `prepared-environment: auto` input of the\n+  # setup-racket action (restore + flagged fallback live inside the\n+  # action, so every call site inherits the same semantics); when the\n+  # producer is skipped (workflow_dispatch override, rollback switch\n+  # RACKET_PREPARED_ARTIFACT=off) or the restore mismatches, the shard\n+  # falls back \u2014 loudly, never silently \u2014 to the full setup path.\n+  # Shard count is governed by repo variable FAST_SHARD_COUNT (default\n+  # 3 = [0, 1, 2]; only the study candidates 4/6/8 widen the matrix \u2014\n+  # the study and its activation rule are recorded in\n+  # docs/reports/test-regression-log.md).\n   # \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n   test:\n-    needs: lint\n+    needs: [lint, fast-env]\n+    # Run via the fallback path whenever fast-env is skipped or failed;\n+    # the guarded restore inside setup-racket plus its fallback marker\n+    # make that visible.\n+    if: ${{ !cancelled() && needs.lint.result == 'success' }}\n     strategy:\n       fail-fast: false\n       matrix:\n-        shard: [0, 1, 2]\n+        # FAST_SHARD_COUNT (W3, guarded study, FAST_SHARD_PLAN\n+        # precedent): default 3 = today's [0, 1, 2]; only the exact\n+        # study candidates 4/6/8 change the matrix; any other value\n+        # (unset, 3, garbage) keeps 3.\n+        shard: ${{ fromJSON(vars.FAST_SHARD_COUNT == '4' && '[0, 1, 2, 3]' || vars.FAST_SHARD_COUNT == '6' && '[0, 1, 2, 3, 4, 5]' || vars.FAST_SHARD_COUNT == '8' && '[0, 1, 2, 3, 4, 5, 6, 7]' || '[0, 1, 2]') }}\n     runs-on: ubuntu-latest\n+    env:\n+      # Resolved once so the legacy package-cache step below and the\n+      # setup-racket call agree: 'auto' only when the prepared path is\n+      # actually in play for this run (producer green, not a dispatch\n+      # version-override, rollback switch not off).\n+      PREPARED_ENV: ${{ (vars.RACKET_PREPARED_ARTIFACT != 'off' && github.event_name != 'workflow_dispatch' && needs.fast-env.result == 'success') && 'auto' || 'off' }}\n     steps:\n       - uses: actions/checkout@v7\n+      # Legacy pre-W3 cache; only pays off on the full path.\n       - name: Cache Racket packages\n+        if: env.PREPARED_ENV != 'auto'\n         uses: actions/cache@v6\n         with:\n           path: ~/.local/share/racket\n           key: racket-pkgs-test-${{ hashFiles('**/info.rkt') }}\n           restore-keys: racket-pkgs-test-\n+      # setup-racket owns the W3 decision: on PREPARED_ENV=auto it first\n+      # attempts the manifest-verified prepared-environment restore and,\n+      # on success, skips install+compile (its step summary reports the\n+      # restore wall clock in place of the package-visible setup\n+      # seconds); on any mismatch or failure it falls back to the full\n+      # path and flags the fallback (::warning + REBUILT summary) \u2014\n+      # never a silent rebuild.\n       - uses: ./.github/actions/setup-racket\n+        id: setup\n         with:\n           racket-version: ${{ github.event.inputs.racket-version || '8.10' }}\n+          prepared-environment: ${{ env.PREPARED_ENV }}\n+          prepared-artifact-name: prepared-env-fast\n+          prepared-installer-sha256: ${{ env.INSTALLER_SHA_LINUX_8_10 }}\n \n       - name: Clear stale bytecode\n+        if: steps.setup.outputs.prepared-env-result != 'success'\n         run: find . -name compiled -type d -not -path './tests/metadata-discovery/fixture/*' -exec rm -rf {} + 2>/dev/null || true\n \n+      # On the prepared-env path the restore action's read-only health\n+      # checks (racket --version, lock verify, (require quickcheck fmt),\n+      # raco fmt --help) replace this gate; recompiling against fresh\n+      # checkout mtimes would defeat the restore.\n       - name: Compile gate (shared deps)\n+        if: steps.setup.outputs.prepared-env-result != 'success'\n         run: raco make main.rkt\n \n-      - name: Run test shard ${{ matrix.shard }}/3\n+      - name: Run test shard ${{ matrix.shard }}/${{ strategy.job-total }}\n         # W7 duration-aware sharding: `active` is evidence-gated. Flip repo\n         # variable FAST_SHARD_PLAN=active ONLY after the shard-plan-report\n         # job below prints `activate` (predicted max-shard duration improves\n@@ -146,7 +221,7 @@ jobs:\n           fi\n           STRICT_TEST_RUNNER=1 racket scripts/run-tests.rkt \\\n             --suite fast --jobs 4 \\\n-            --shard-index ${{ matrix.shard }} --shard-total 3 \\\n+            --shard-index ${{ matrix.shard }} --shard-total ${{ strategy.job-total }} \\\n             $PLAN_ARGS \\\n             --json-out test-results.json \\\n             2>&1 | tee test-output.log\n@@ -155,7 +230,7 @@ jobs:\n         if: always()\n         run: |\n           {\n-            echo \"## Test results \u2014 fast shard ${{ matrix.shard }}/3\"\n+            echo \"## Test results \u2014 fast shard ${{ matrix.shard }}/${{ strategy.job-total }}\"\n             if [ -f test-results.json ] && command -v jq >/dev/null 2>&1; then\n               jq -r '\"- Files: \\(.run_summary.file_count)  pass=\\(.run_summary.pass) fail=\\(.run_summary.fail) timeout=\\(.run_summary.timeout) skip=\\(.run_summary.skip)\",\n                      \"- Wall clock: \\(.run_summary.wall_clock_seconds)s  mode=\\(.run_summary.execution_mode)  profile=\\(.run_summary.profile)  shard=\\(.run_summary.shard)\",\n@@ -254,17 +329,35 @@ jobs:\n             echo \";; No duration artifacts available (upstream gate failed or artifacts missing); skipping plan build.\" | tee shard-plan-report.log\n             exit 0\n           fi\n+          # Current shard total = number of staged per-shard duration JSONs\n+          # (3 today; N once FAST_SHARD_COUNT is activated per the ledger).\n+          CURRENT_TOTAL=$(ls -1 durations/*.json | wc -l)\n           racket scripts/run-tests.rkt \\\n-            --suite fast --shard-total 3 \\\n+            --suite fast --shard-total \"$CURRENT_TOTAL\" \\\n             --shard-plan report --durations durations \\\n             2>&1 | tee shard-plan-report.log\n+          # FAST_SHARD_COUNT study (v1.00.16 W3, report-only): predicted\n+          # max shard at N=4/6/8 over this run's post-W1/W2 durations.\n+          # Activation rule (FAST_SHARD_PLAN precedent): activate N only\n+          # if the predicted max shard improves AND the runner-minute\n+          # cost delta is recorded and accepted in\n+          # docs/reports/test-regression-log.md.\n+          echo \";; FAST_SHARD_COUNT study (W3, report-only): predicted max shard at N=4/6/8\" | tee -a shard-plan-report.log\n+          for n in 4 6 8; do\n+            [ \"$n\" = \"$CURRENT_TOTAL\" ] && continue\n+            echo \";; ---- N=$n ----\"\n+            racket scripts/run-tests.rkt \\\n+              --suite fast --shard-total \"$n\" \\\n+              --shard-plan report --durations durations 2>&1 \\\n+              | grep -E '^shard [0-9]+/[0-9]+:|^;; ' || echo \";; (planner produced no report for N=$n)\"\n+          done | tee -a shard-plan-report.log\n       - name: Post predicted-vs-observed summary\n         if: always()\n         run: |\n           {\n             echo \"## Shard plan report (W7, report-only)\"\n             echo '```'\n-            grep -E 'shard [0-9]+/3:|^;; ' shard-plan-report.log || true\n+            grep -E 'shard [0-9]+/[0-9]+:|^;; ' shard-plan-report.log || true\n             echo '```'\n             if command -v jq >/dev/null 2>&1 && ls durations/*.json >/dev/null 2>&1; then\n               echo \"- Observed round-robin max shard wall clock: ${OBSERVED}s\"\n@@ -275,6 +368,7 @@ jobs:\n               echo \"- Observed round-robin max shard wall clock: n/a (no duration artifacts in this run)\"\n             fi\n             echo \"- Activation: set repo variable \\`FAST_SHARD_PLAN=active\\` only when the recommendation above is \\`activate\\`.\"\n+            echo \"- FAST_SHARD_COUNT (W3, report-only): predicted max shard for N=4/6/8 printed above from the same durations. Default stays 3; activate N only if the predicted max shard improves AND the runner-minute cost delta is recorded and accepted in \\`docs/reports/test-regression-log.md\\` (activation + revert procedure recorded there).\"\n           } >> \"$GITHUB_STEP_SUMMARY\"\n         env:\n           OBSERVED: ${{ steps.observed.outputs.observed }}"
  },
  {
    "sha": "041f3f4bbfefbf558767809af4328ae107772b7b",
    "filename": "docs/reports/CI-RACKET-CACHE-POLICY.md",
    "status": "modified",
    "additions": 5,
    "deletions": 2,
    "changes": 7,
    "blob_url": "https://github.com/coinerd/q/blob/1351fb0568d349b96d3aa9083138138419e38548/docs%2Freports%2FCI-RACKET-CACHE-POLICY.md",
    "raw_url": "https://github.com/coinerd/q/raw/1351fb0568d349b96d3aa9083138138419e38548/docs%2Freports%2FCI-RACKET-CACHE-POLICY.md",
    "contents_url": "https://api.github.com/repos/coinerd/q/contents/docs%2Freports%2FCI-RACKET-CACHE-POLICY.md?ref=1351fb0568d349b96d3aa9083138138419e38548",
    "patch": "@@ -13,9 +13,12 @@ The full-regression platform lane previously cached `~/.racket` and workspace by\n | Cache key | `racket-addon-v2`, runner OS, Racket x64/CS/full distribution tuple, Racket version, `info.rkt`, and `ci/racket-package-lock.rktd` form the exact key. |\n | Restore policy | No prefix restore keys are permitted. A manifest or lock change produces a clean store. |\n | Workspace bytecode | `compiled/` directories are deleted before setup and are never cached. |\n-| Compile boundary | Every run, including an exact cache hit, relinks q and executes `raco setup --no-docs --jobs 4 --pkgs q fmt`, then checks `raco fmt --help`. |\n+| Compile boundary | Default: every run, including an exact cache hit, relinks q and executes `raco setup --no-docs --jobs 4 --pkgs q fmt`, then checks `raco fmt --help`. W3 ubuntu exception: see \"Prepared environment\" below. |\n+| Prepared environment (W3) | The ubuntu fast-gate test shards (`.github/workflows/ci.yml` `test` job calling `setup-racket` with `prepared-environment: auto`; producer job `fast-env`) restore the prepared artifact produced by `prepare-racket-environment` (24h immutable: `addon-store/`, `q-compiled/`, `manifest.json`). `restore-racket-environment` verifies the manifest against the job tuple \u2014 schema, repo, SHA, OS, arch, Racket tuple, lock digest, source digest \u2014 BEFORE any test. A verified restore replaces the install + compile boundary for that job (the restore's four read-only health checks \u2014 `racket --version`, lock verify, `(require quickcheck fmt)`, `raco fmt --help` \u2014 replace the compile health probe). Any mismatch, missing artifact, or producer failure falls back to the full path above with a `::warning` and a REBUILT step summary \u2014 never silently. Rollback switch: repo variable `RACKET_PREPARED_ARTIFACT=off`. |\n+| Lane split | **ubuntu vs macOS**: only the ubuntu fast-gate lanes may skip compile on a manifest-verified prepared-environment restore (their fallback path still compiles). The macOS platform lanes have NO prepared-environment path: they keep compile-on-every-hit unconditionally per the v1.00.10 release condition. |\n+| Shard policy | Fast-shard count is governed by repo variable `FAST_SHARD_COUNT` (default `3` = `[0, 1, 2]`; only the study values `4`/`6`/`8` widen the matrix). The report-only study, activation rule, and revert command live in `docs/reports/test-regression-log.md` (`FAST_SHARD_PLAN` precedent). |\n \n-> **Binding constraint:** A cache hit accelerates dependency acquisition. It never authorizes skipping compilation of package-visible q modules.\n+> **Binding constraint:** A cache hit accelerates dependency acquisition. It never authorizes skipping compilation of package-visible q modules. The single W3 exception is the manifest-verified prepared-environment restore on ubuntu fast-gate lanes (row above): the artifact was compiled from identical lock + source digests and is verified before any test, with a loudly flagged rebuild fallback. macOS platform lanes have no such exception.\n \n ## Dependency lock maintenance\n "
  },
  {
    "sha": "ce36c08e6d909874c78a61e98a73d3bbe6e59911",
    "filename": "docs/reports/test-regression-log.md",
    "status": "modified",
    "additions": 64,
    "deletions": 0,
    "changes": 64,
    "blob_url": "https://github.com/coinerd/q/blob/1351fb0568d349b96d3aa9083138138419e38548/docs%2Freports%2Ftest-regression-log.md",
    "raw_url": "https://github.com/coinerd/q/raw/1351fb0568d349b96d3aa9083138138419e38548/docs%2Freports%2Ftest-regression-log.md",
    "contents_url": "https://api.github.com/repos/coinerd/q/contents/docs%2Freports%2Ftest-regression-log.md?ref=1351fb0568d349b96d3aa9083138138419e38548",
    "patch": "@@ -587,3 +587,67 @@ baseline, retained v1.00.10 L4 proof. Release notes for `v1.00.11`:\n > (report-only) with the recorded NO-GO cutover decision and\n > `RACKET_PREPARED_ARTIFACT=off` rollback switch; and retained the deterministic\n > test-feedback baseline. No required gate was weakened.\n+\n+## v1.00.16 W3 \u2014 prepared-environment fast-gate cutover (ubuntu) + FAST_SHARD_COUNT study (2026-08-24)\n+\n+W3 cuts fixed fast-gate overhead on two axes: (a) the v1.00.11\n+prepared-environment pilot's validated artifact flow is now wired into the\n+PR fast gate's ubuntu lanes (go/no-go re-evaluation WITH cutover authority,\n+scoped to ubuntu only), and (b) a guarded shard-count study runs report-only\n+behind the new repository variable `FAST_SHARD_COUNT` (default `3` = today),\n+following the `FAST_SHARD_PLAN` precedent exactly (default off, guarded\n+activation with an observed post-activation check run, revert command here).\n+\n+### Prepared-environment cutover (ubuntu fast lanes) \u2014 GO, hosted observation pending\n+\n+What changed in `.github/workflows/ci.yml` + `.github/actions/setup-racket`:\n+\n+| Element | Change |\n+|---|---|\n+| New producer job `fast-env` | ubuntu-only, first fast-gate job: installs Racket + exact-key addon store, then `prepare-racket-environment` builds the 24h immutable artifact (`addon-store/`, `q-compiled/`, `manifest.json`) via `actions/cache/save`. Never blocks verdicts: consumers fall back to full `setup-racket` when no verified artifact restores. |\n+| `setup-racket` input `prepared-environment` | `off` (default) = legacy path byte-identical (every existing call site unchanged); `auto` = guarded prepared-env flow. Only the three ubuntu fast-gate `test` shards opt in; the macOS `test-platform` lane, cross-version lane, and workflows lane do NOT (they keep install + relink + compile on every run). |\n+| Restored-path compile skip | On verified restore, `setup-racket` skips `raco setup --no-docs` compile (and the addon-store restore + install that precede it), and runs the four read-only health checks (`racket --version`, lock verify, `(require quickcheck fmt)`, `raco fmt --help`). Output `prepared-environment=restored` is written to `$GITHUB_OUTPUT` and `$GITHUB_STEP_SUMMARY` (W0 setup/execution split evidence). |\n+| Guarded fallback (never silent rebuild) | Any mismatch (no producer output, addon cache miss, manifest verify failure) runs the FULL legacy path \u2014 restore + install + relink + `raco setup --pkgs q fmt` \u2014 and marks `prepared-environment=rebuild-fallback` in outputs + step summary. Mismatch inside `restore-racket-environment` remains a hard failure; only the \"no artifact present\" case falls back. |\n+| Rollback switch | Repository variable `RACKET_PREPARED_ARTIFACT=off` disables the producer and forces `auto` \u2192 `off` at all call sites (single switch, per the pilot report's contract). Command: `gh api -X PUT repos/coinerd/q/actions/variables/RACKET_PREPARED_ARTIFACT -f value=off`. |\n+\n+Decision basis: the pilot's integrity tooling is complete and locally verified\n+(manifest `emit`/`verify`/`digest`, SHA-256-verified installer archive,\n+read-only restore contract with zero package-mutation commands \u2014 invariants\n+I9/I10 respected); the only NO-GO reason at v1.00.11 was zero hosted-run\n+samples. W3 therefore cuts over the ubuntu fast lanes behind the guarded\n+fallback and records the hosted evidence requirement:\n+\n+| Observation gate (required to keep GO) | Requirement |\n+|---|---|\n+| Two consecutive fast-gate CI runs on one PR | Run 1 = cold (producer builds artifact, shards fall back to full setup \u2014 expected `rebuild-fallback` or producer-parallel full path); run 2 = warm: verified prepared-env restore on all three shards with `prepared-environment=restored` and a materially reduced setup phase, visible in the retained artifacts' W0 setup/execution split. |\n+| Failure handling | Any warm-run restore mismatch is a hard failure (never silent); timeout is never reported as a pass. |\n+| Breach action | Revert with `RACKET_PREPARED_ARTIFACT=off`; record numbers here. |\n+\n+The macOS platform lane's v1.00.10 release condition (compile on every cache\n+hit) is unchanged and now recorded as an explicit lane split in\n+`docs/reports/CI-RACKET-CACHE-POLICY.md`.\n+\n+### FAST_SHARD_COUNT study \u2014 KEEP 3 (report-only phase live; activation pending first study artifact)\n+\n+`FAST_SHARD_COUNT` is a new repository variable, default `3` = today's\n+behavior (matrix literal in `ci.yml` unchanged until an activation decision\n+is recorded here). The `shard-plan-report` job now ALSO runs the\n+duration-aware planner over this run's post-W1/W2 staged durations at\n+`N=4/6/8` (`--shard-total 4|6|8`, report-only) and appends the predicted\n+max-shard table to the retained `shard-plan-report` artifact \u2014 the same\n+retained-artifact mechanism the `FAST_SHARD_PLAN` decision used (run\n+32297737631 \u2192 ledger entry 2026-08-19).\n+\n+| Field | Value |\n+|---|---|\n+| Study variable | `FAST_SHARD_COUNT` (repo variable; `3` = default/keep, activation value = the chosen N, which must equal the `ci.yml` matrix literal after the activation edit) |\n+| Study mechanism | `shard-plan-report` job: `racket scripts/run-tests.rkt --suite fast --shard-total <N> --shard-plan report --durations durations` per N in {4,6,8}, report-only, retained in the `shard-plan-report` artifact |\n+| Activation rule (all must hold) | (1) predicted max shard improves vs N=3 over the same run's durations; (2) inventory preserved (same total files, planner `substituted` recorded); (3) runner-minute cost delta computed from observed job minutes (fast-gate job minutes at N minus at 3) and explicitly accepted here; (4) observed post-activation check run recorded here with no material max-shard regression |\n+| Decision (2026-08-24) | **KEEP `3`** \u2014 report-only phase wired; no hosted study artifact with post-W1/W2 4/6/8 predictions has been transcribed yet, so no activation criterion is evaluable. Activation requires the table + accepted cost delta recorded in a dated addendum below. |\n+| Revert command | `gh api -X PUT repos/coinerd/q/actions/variables/FAST_SHARD_COUNT -f value=3` and restore the `ci.yml` fast-gate matrix to `[1, 2, 3]` (the variable alone does not resize the matrix; the matrix literal is the source of truth) |\n+\n+### Constraints reaffirmed\n+\n+Gate structure (fast / platform / workflows / cross-version), job names, and\n+required status checks are unchanged; the workflows contain zero\n+impact-selector flags (verify gate: `grep -rn \"changed-base\\|changed-head\\|impact-dry-run\" q/.github/workflows/ \\| wc -l` = 0); a timeout is never reported as a pass."
  }
]
