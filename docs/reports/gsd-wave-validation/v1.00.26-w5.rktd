(kind . "wave-validation")
(schema-version . 2)
(campaign . "v1.00.26")
(wave . "W5")
(validated-artifact . "docs/reports/gsd-wave-evidence/v1.00.26-w5.rktd")
(implementation-sha . "53e56e2c83ad4843dd3d1a85854fe8126ab623e3")
(branch . "campaign/v1.00.26-w5")
(validation-result . pass)
(checks . ((runtime-contract-tests . "racket tests/test-ci-runtime-contract.rkt — 42 success(es) 0 failure(s) 0 error(s) at implementation-sha (Pin 11: guarded WORKFLOW_TEST_JOBS seam with the exact vars.WORKFLOW_TEST_JOBS == '4' expression; --jobs \"$WORKFLOW_TEST_JOBS\" flow-through; no unconditional --jobs 4; --shard-index/--shard-total 2 stability so the required-check graph is untouched; W5 decision comment with guard label, HOLD, artifact pointer, and rollback; stress-evidence.json exists, is schema q.ci-topology.workflow-worker-stress/1-bound, records p50 target 220 unrevised, real-ci-four-worker-pr-runs=0, gate-evaluable=false, cross-worker-contamination-observed=false, non-trivial missing-for-activation, and is byte-bound by SHA256SUMS; contamination canary negative on live ci.yml and positive on all three fixture classes)")
           (worker-security-tests . "racket tests/test-worker-security.rkt — 40 + 2 success(es) 0 failure(s) 0 error(s); the plan's conditional-extension clause resolved to 'extended': no workflow-level worker-isolation pin existed before W5, so the isolation sub-suite (live ci.yml free of shared output names / shard-less cache keys / overlapping temp roots, positive controls for each class, per-worker HOME/workdir separation language) was added there, and the extension is a recorded digest input")
           (sums-binding . "sha256sum -c artifacts/ci-topology/v1.00.26-w5/SHA256SUMS — OK; stress-evidence.json matches byte-for-byte (verified in-suite and standalone)")
           (digest-binding . "non-artifact digest 0c7fdc2080cf7f9923dd13ddc501c5e8d53702283c3ff35b459bb1317b039667 reproduces with the recorded command at implementation-sha 53e56e2c83ad4843dd3d1a85854fe8126ab623e3 over .github/workflows/ci.yml tests/test-ci-runtime-contract.rkt tests/test-worker-security.rkt")
           (branch-guard . "test \"$(git branch --show-current)\" != \"main\" — passes; work is on campaign/v1.00.26-w5, delivery goes through the protected-main PR")
           (run-block-shape . "the workflows shard run block was extracted from ci.yml and checked two ways: bash -n parses the reconstructed command (exit 0), and a guarded dry-run reproduction shows the interpreter receiving the full flag set including --shard-index/--shard-total/--json-out — proving the HOLD comment placement above the continued command keeps the runner command intact (the attempt-1 shape, comments inside the continuation, was shown to terminate the command at the '#' line)")
           (format-lint . "raco fmt applied to both touched test files produced no diffs at implementation-sha (files are fmt-clean; no reformat was needed or applied, so the digest binding is unaffected)")
           (metrics-sync . "the generated README metrics table was re-synced via racket scripts/metrics.rkt before the final commit so the CI lint-quality equality check holds; the evidence trio adds record-only files outside the counted surfaces")
           (yaml-parse . "the edited ci.yml parses clean (YAML safe_load at implementation-sha) — the comment relocation is inside a run block scalar, so workflow structure is unchanged")
           (rollback-rehearsal-dry-run . "documented sequence (not executed against live protection, and none needed): the delivered worker-count behavior is byte-identical to the pre-wave product default of 2 unless a repo variable is set, so there is nothing to roll back functionally; if WORKFLOW_TEST_JOBS=4 were ever activated, the documented one-line rollback is unsetting the repo variable (or setting it to anything but '4'), which the guard resolves back to 2 on the next run; reverting the seam commit restores the exact pre-wave workflow tree; a checkpoint/SHA256SUMS mismatch is caught by sha256sum -c failing closed")
           (file-contract . "all eight declared wave targets accounted for: ci.yml carries the guarded seam and the HOLD decision comment; test-ci-runtime-contract.rkt extended with Pin 11 (guard, decision, artifact binding, canary); test-worker-security.rkt extended with the W5 isolation sub-suite per the conditional clause; stress-evidence.json + SHA256SUMS present and checksum-bound; the three rktd records are record-only additions outside the digest inputs; delivery lands via the campaign PR on campaign/v1.00.26-w5 with the squash-merge and post-merge gate confirmation owned by the runtime coordinator")))
(record-only . "this validation adds no code and touches no digest input")
