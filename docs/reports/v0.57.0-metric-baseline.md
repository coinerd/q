# v0.59.5 Metric Baseline — Measurement + Characterization Truth

**Date**: 2026-05-24
**Milestone**: v0.59.5 (#495)
**Status**: Complete

## Baseline Metrics

### Contract Metrics
- **Total `any/c` in contract-out**: 882 across 148 files
- **Top offenders**: loop-phases.rkt (60), racket-tooling-helpers.rkt (53), github/helpers.rkt (28)

### Architecture Boundary Metrics
- **Runtime upward import exceptions**: 16 (max allowed)
- **Runtime subdirectory files**: 21 in subdirs (context-assembly/ 5, iteration/ 13, session-index/ 3)
- **Total runtime .rkt files**: 73 (52 flat + 21 subdirs)
- **5 subdirectory upward imports discovered**: iteration/loop-config.rkt, iteration/loop-phases.rkt, iteration/main-loop.rkt, iteration/step-interpreter.rkt, runtime/layer-adapters.rkt

### Hotspot Top 10 (score = frequency × LOC)
| File | Freq | LOC | Score |
|------|------|-----|-------|
| runtime/agent-session.rkt | 69 | 450 | 31050 |
| tui/tui-keybindings.rkt | 39 | 633 | 24687 |
| runtime/session-lifecycle.rkt | 41 | 510 | 20910 |
| tui/tui-render-loop.rkt | 38 | 465 | 17670 |
| extensions/gsd-planning.rkt | 70 | 246 | 17220 |
| scripts/run-tests.rkt | 22 | 765 | 16830 |
| tools/scheduler.rkt | 34 | 475 | 16150 |
| tui/commands.rkt | 41 | 391 | 16031 |
| extensions/loader.rkt | 35 | 386 | 13510 |
| runtime/session-store.rkt | 31 | 418 | 12958 |

### Characterization Test Counts
| Test File | Tests | Area |
|-----------|-------|------|
| test-gsd-production-session-isolation.rkt | 8 | GSD session global leakage |
| test-tui-hotspot-characterization.rkt | 8 | TUI decomposition targets |
| test-retry-error-classification.rkt | 8 | Error classification gaps |
| test-hotspot-report.rkt | 5 | Hotspot metric infrastructure |
| test-arch-boundaries.rkt | 3 | Recursive boundary scanning |
| **Total new** | **32** | |

## Key Findings (documented for remediation)

### v0.59.5 — GSD Session Isolation
- `gsd-default-ctx` is module-level global singleton in `session-state.rkt`
- `state-machine.rkt` directly accesses `gsd-default-ctx` at 6 call sites
- `current-gsd-*` / `set-gsd-*` API all operate on shared global
- Independent `make-gsd-context` instances ARE properly isolated (API supports it)
- Production code doesn't use the per-session API

### v0.59.5 — Runtime Boundaries + Resilience
- **BUG**: Structured 5xx provider-errors (category `'server`) are NOT retryable
  - `retryable-error?` checks for `'server-error` but `classify-http-status` returns `'server`
  - String-based 5xx IS retryable via fallback — inconsistent behavior
  - Fix: change `'server-error` → `'server` in memq list

### v0.59.5 — TUI Hotspot Decomposition
- **BUG**: `handle-user-submit!` contract lies — claims `(-> tui-ctx? string? tui-ctx?)` but returns thread
  - Contract throws `exn:fail:contract?` on every call
  - Prevents testing the function
  - Fix: change to `(-> tui-ctx? string? void?)` or `(-> tui-ctx? string? thread?)`
- Top TUI hotspots: keybindings (24687), render-loop (17670), commands (16031)

### v0.59.5 — Contract Precision
- 882 `any/c` across 148 files
- v0.59.5 will target top-5 offenders for typed boundary pilot

## Waves Completed
- W0 (#5106): Recursive arch boundary baseline — PR #5112
- W1 (#5107): Hotspot report baseline + CI annotation — PR #5113
- W2 (#5108): GSD production session isolation characterization — PR #5114
- W3 (#5109): TUI hotspot characterization + submit contract decision — PR #5115
- W4 (#5110): Retry/error classification characterization — PR #5116
- W5 (#5111): Metric baseline + milestone gate (this wave)
