# Broad-Suite W0 Failure Clusters

Reproduced remaining broad-suite failures and grouped root causes:

- Contract cluster A: `runtime/iteration/directive.rkt`, `interfaces/rpc-mode.rkt`, `tui/tui-render-loop.rkt`
- Integration pipeline cluster: tool-call-id contract failure in `runtime/tool-coordinator.rkt` surfaces as `'error` in integration/smoke/golden tests
- UI contract cluster: `tui/state-ui.rkt`, `extensions/ui-surface.rkt`
- Command parse shape drift: `tests/tui/test-command-integration.rkt`
- Examples/tooling cluster: `examples/sdk/06-sessions.rkt`, `extensions/racket-tooling-helpers.rkt`
- Version-gate drift: `tests/test-self-hosting-deep.rkt`

Wave mapping:
- W1 = Contract cluster A
- W2 = Integration/UI/command/version clusters
- W3 = Examples/tooling + release drift
- W4 = full gate + release
